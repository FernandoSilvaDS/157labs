theme: THEME-NAME', replacing _THEME-NAME_ with the theme name, as shown in the theme repository '_config.yml' (most themes follow a 'jekyll-theme-NAME' naming convention). For a list of supported themes, see "[Supported themes](https://pages.github.com/themes/)" on the GitHub Pages website. For example, to select the Minimal theme, type 'theme: jekyll-theme-minimal'. * To use any other Jekyll theme hosted on GitHub, type 'remote_theme: THEME-NAME'
